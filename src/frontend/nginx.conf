server {
    listen 80; # Nginx di kontainer listen di port 80
    server_name localhost; # Atau nama domain Anda jika ada

    # Enable logging for debugging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log debug;

    # Menyajikan file statis frontend dari root
    location / {
        root   /usr/share/nginx/html;
        index  index.html index.htm;
        try_files $uri $uri/ /index.html; # Penting untuk routing SPA (React/Vue/Angular)
    }    # Me-reverse proxy semua request yang dimulai dengan /api/ ke layanan backend
    location /api/ {
        # Use direct backend address from environment variable or default to backend:8080
        # This will be replaced at runtime by the entrypoint script
        set $backend_upstream "backend:8080";
        
        # Use resolver for Docker's DNS
        resolver 127.0.0.11 valid=30s;
        
        # Use proxy_pass with variable
        proxy_pass http://$backend_upstream;
        
        # Add debug headers
        add_header X-Debug-Target $backend_upstream always;
        
        # Add CORS headers
        add_header 'Access-Control-Allow-Origin' '*' always;
        add_header 'Access-Control-Allow-Methods' 'GET, POST, OPTIONS' always;
        add_header 'Access-Control-Allow-Headers' 'DNT,User-Agent,X-Requested-With,If-Modified-Since,Cache-Control,Content-Type,Range' always;
        
        # Standard proxy headers
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }

    # (Opsional) Pengaturan error page
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }
}